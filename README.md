# vite-demo

> vue-next + vite 学习路程

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build

#tip：
vue-router need the latest version which >= 4.0.0-beta.2  
To get the versions need run 'npm info vue-router versions'
And get the version which in your pc need run 'npm info vue-router version'

For a detailed explanation on how things work, check out the [guide](https://github.com/vitejs/vite/) and [docs for vue-loader](https://github.com/vuejs/vue-next/).
